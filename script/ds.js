(function (w) {

    "use strict";

    w.ds = {
        debugMode: false
    };
    w.ds.gui = {
        alert: function (message) {
            window.alert(message);
        }
    };
    w.ds.model = {};
    w.ds.store = {};
    w.ds.util = {};
    w.ds.page = {};
}(window));